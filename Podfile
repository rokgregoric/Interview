project 'Interview.xcodeproj', {
  'Debug' => :debug,
  'Release' => :release,
}

platform :ios, '11.0'

inhibit_all_warnings!

target 'Interview' do
  use_frameworks!

  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwifterJSON', git: 'https://github.com/rokgregoric/SwifterJSON.git'
end
